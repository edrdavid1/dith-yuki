[package]
name = "dither-zip-safe"
version = "0.1.0"
edition = "2021"
description = "Shared secure ZIP primitives for the app loader and OS preview extractors"
license = "FCL-1.0"

[dependencies]
thiserror = "1.0"
unicode-normalization = "0.1"

[lints.rust]
unsafe_code = "forbid"
