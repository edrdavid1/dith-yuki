[package]
name = "engine-registry"
version = "0.1.0"
edition = "2021"
description = "Algorithm registry: FilterAlgorithm trait, AlgorithmId, FilterContext, and schema types"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
engine-tiles = { path = "../engine-tiles" }
